import 'package:flutter/material.dart';

class ChakraScreen extends StatelessWidget {
  const ChakraScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("7 Chakra Screen"),
    );
  }
}
